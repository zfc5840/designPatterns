1、普通代理：调用者只知代理而不用知道真实的角色是谁，屏蔽了真实角色的变更对高层模块的影响，真实主题角色想怎么改就怎么改，对高层的模块没有任何影响
，只要你实现了接口所对应的方法，该模式非常适合对扩展性要求较高的场所。
2、强制代理：不能直接获取代理对象，而是通过被代理对象获取代理者，人后代理者执行代理任务，比如明星指定代理。
3、代理是有个性的：一个类可以通过实现多个接口，也就是能完成多个任务。
4、虚拟代理：也就是代理者在执行相应任务时，判断被代理者是否存在。
5、动态代理：Spring的AOP功能就是。